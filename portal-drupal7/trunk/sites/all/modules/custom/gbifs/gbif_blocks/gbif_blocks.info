name = GBIF Blocks
description = A module to emit various content in blocks (so that we don't use templates for that).
core = 7.x
version = 7.x-0.1
package = GBIFS

files[] = gbif_blocks.module

dependencies[] = gbif_tweaks